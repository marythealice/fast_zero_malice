name: Pipeline
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:
      - name: Copies files from the repo
        uses: actions/checkout@v3

      - name: Install PythonD
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install Poetry
        run: pipx install poetry
      
      - name: Install dependencies
        run: poetry install